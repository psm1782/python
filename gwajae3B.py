# coding=utf-8

l1 = tuple(raw_input('Enter the name, age, score:').split(','))
l2 = tuple(raw_input('Enter the name, age, score:').split(','))
l3 = tuple(raw_input('Enter the name, age, score:').split(','))
l4 = tuple(raw_input('Enter the name, age, score:').split(','))
l5 = tuple(raw_input('Enter the name, age, score:').split(','))
 
 
LisT = [l1, l2, l3, l4, l5]
 
LisT.sort()
 
print LisT
